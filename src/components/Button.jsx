import React from 'react'

function Button () {
  return (
            <button onClick={() => {
              console.log(123)
            }}>
                test
            </button>
  )
}

export default Button
